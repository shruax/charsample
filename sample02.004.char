// 示例：浮点数数据类型

// 用带有小数点的数字进行赋值，系统将默认其为浮点数数据类型（float）
f1 := 1.32

pl("%v, %v", typeOf(f1), f1)

previus_f1 := f1

f1 = f1 * 0.8

// 内置函数pr与其他语言中的print类似，直接输出一个或多个表达式的值，注意多个值之间没有空格分隔，最后也不输出换行
pr(previus_f1, "*", 0.8, "=", f1)

// 不带参数的pln函数效果是输出一个换行符
pln()

f2 := 0.93

// 察语言支持“+=”、“-=”、“*=”、“/=”等二元运算符，`f2 /= 0.3` 相当于 `f2 = f2 / 0.3`
f2 /= 0.3

pr(0.93, "/", 0.3, "=", f2, "\n")
