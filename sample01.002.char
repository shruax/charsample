// 示例：基本赋值与计算

// 察语言中与Go语言一样，使用“:=”操作符对变量进行快速声明与赋值，这种方式叫做短变量声明或者快速变量声明
x := 1.25
y := x + 1

pln(x + y)

// 也可以用var来声明变量，然后用“=”操作符来赋值
var s1

s1 = "abc"

var s2 = "123"

var s3 = s1 + s2

// pln函数如果带有多个参数将逐一输出，并以一个空格作为间隔
pln("s3:", s3)

// pass是一个特殊的内置函数，没有任何作用，一般用于占位等
pass()
